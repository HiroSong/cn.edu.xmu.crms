<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.edu.xmu.crms.dao.DeadlineDao" >

    <select id="selectCourseDeadlineById"  resultType="Deadline" parameterType="BigInteger">
        SELECT * FROM deadline WHERE course_id = #{id}
    </select>

    <select id="selectDeadlineIdByCourseId"  resultType="BigInteger" parameterType="BigInteger">
        SELECT deadline_id FROM course_deadline WHERE course_id = #{id}
    </select>
</mapper>
